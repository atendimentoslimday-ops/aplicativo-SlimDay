import React from "react";
import Sales from "./Sales";

const Index = () => {
  // Nesta pasta (Vendas), o Index apenas renderiza a página de vendas.
  // Não há redirecionamento para o aplicativo aqui.
  return <Sales />;
};

export default Index;
